import Interface from "@/components/Interface";

const Home = () => {
  return (
    <main className={`flex min-h-screen bg-yellow-100`}>
      <Interface />
    </main>
  );
};

export default Home;
