import Interface from "@/components/Interface";

const Home = () => {
  return (
    <main className={`flex min-h-screen bg-amber-50 font-ibmPlexMono`}>
      <Interface />
    </main>
  );
};

export default Home;
